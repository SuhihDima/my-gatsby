import * as React from "react";
import { Link } from "gatsby";
import Layout from "../components/layout";

export const ContactPage = () =>{
    return(
        <Layout PageHeading='Контакты' PageTitle='Свяжитесь с нами'>
            Наши контакты 
        </Layout>
    );
};

export default ContactPage;